describe('useOnClick', () => {
  test.todo('navigates to the history page');
});

describe('useOnSubmit', () => {
  test.todo('submits the form');
  test.todo('passes the parsed search terms');
  describe('when nothing was entered', () => {
    test.todo('does nothing');
  });
});

describe('useSearch', () => {
  test.todo('returns search state');
  test.todo('returns the parsed name from the search');
});
